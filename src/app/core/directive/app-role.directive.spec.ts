import { AppRoleDirective } from './app-role.directive';

describe('AppRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRoleDirective();
    expect(directive).toBeTruthy();
  });
});
